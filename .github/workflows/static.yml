
on:
 push:
  branches: ["main"]

jobs: 
  download-data:
    runs-on: ubuntu-latest
    environment: Andmed
    steps:
    - name: Checkpout repo
      uses: actions/checkout@v2
    
    - name: Install Minio client
      run: |
       wget https://dl.min.io/client/mc/release/linux-amd64/mc
       chmod +x mc
       sudo mv mc /usr/local/bin
    
    - name: Configure Minio client
      shell: bash
      env:
       HOST: ${{ secrets.GRIPIRADAR_HOST }}
       ACCESS_KEY: ${{ secrets.GRIPIRADAR_ACCESS_KEY }}
       SECRET_KEY: ${{ secrets.GRIPIRADAR_SECRET_KEY }}
      run: |
       mc alias set myminio $HOST $ACCESS_KEY $SECRET_KEY
    
    - name: Downloaad data from bucket
      run: |
       mc cp --recursive myminio/gripiradar-ut-ee ./

  render:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run quarto
      run: |
       docker run taavipall/gripiradar Rscript -e "library(quarto); quarto_render("index.qmd")"
    
    - name: List results
      run: |
       ls -l

  